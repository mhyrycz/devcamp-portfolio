<h1>angular portfolio item <h1/>

<%= link_to "Create new item", new_portfolio_url if logged_in?(:site_admin)%>

<%= render partial: "portfolio_item", collection: @angular_portfolio_items %>


<%- #rake routes prefix: edit_portfolio uri:  /portfolios/:id/edit(.:format) -%>
